import Foundation
import CoreData

extension Task {

    var allTags: [Tag] {
//        (self.tags as? Set<Tag> ?? [])
//            .sorted { ($0.name ?? "") <= ($1.name ?? "") }
        []
    }

}
